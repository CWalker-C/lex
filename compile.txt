（保留字，int）
（标识符，main）
（操作符/界符，(）
（保留字，void）
（操作符/界符，)）
（操作符/界符，{）
（保留字，int）
（标识符，i）
（操作符/界符，==）
（常数，100）
（操作符/界符，;）
（标识符，printf）
（操作符/界符，(）
（操作符/界符，"）
（标识符，hello）
（标识符，world）
（操作符/界符，）
（操作符/界符，~）
（操作符/界符，\）
（标识符，n）
（操作符/界符，"）
（操作符/界符，)）
（操作符/界符，;）
（保留字，return）
（常数，0）
（操作符/界符，;）
（操作符/界符，}）
第1个标识符:main
第2个标识符:i
第3个标识符:printf
第4个标识符:hello
第5个标识符:world
第6个标识符:n
